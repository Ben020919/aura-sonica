# ── Page 6 留言自動寄信（EmailJS）──────────────────────
# Venus 去 https://www.emailjs.com 免費開戶，攞到以下 3 條 key，
# 複製呢個檔做 .env（同層），填返落嚟，重新 npm run dev 就會自動寄到
# VENUSLEUNG412@GMAIL.COM，唔使開訪客自己嘅郵件 app。
#
# 未填之前，留言會用 mailto 後備方式（打開訪客郵件 app 寄出），一樣 work。

VITE_EMAILJS_SERVICE_ID=
VITE_EMAILJS_TEMPLATE_ID=
VITE_EMAILJS_PUBLIC_KEY=

# ── 後端 API 網址 ──────────────────────────────────
# 本機開發唔使填（預設用 http://localhost:8000）。
# 上 Render 部署時，喺前端 Static Site 設定填後端 web service 網址，
# 例如 https://aura-sonica-api.onrender.com
VITE_API_URL=
